import './x123456.js'
import './y123456.js'
import './z123456.js'
import './matrixX.js'
import './matrixY.js'
import './matrixZ.js'
import './abcdef.js'
class matrixabcdef{
      [`a + b + c + d + e + f`]
      [`a - b - c - d - e - f`]
      [`a * b * c * d * e * f`]
      [`a % b % c % d % e % f`]
      [`a / b / c / d / e / f`]
      [`a ^ b ^ c ^ d ^ e ^ f`]
}